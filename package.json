{
  "name": "crosscode",
  "private": true,
  "packageManager": "pnpm@11.17.0",
  "scripts": {
    "build": "tsc --noEmit && pnpm --filter crosscode-vscode-extension build",
    "test": "vitest run --coverage",
    "test:postgres": "node -e \"if (!process.env.CROSSCODE_TEST_DATABASE_URL) { console.error('CROSSCODE_TEST_DATABASE_URL is required'); process.exit(1) }\" && vitest run apps/service/src/store.integration.test.ts apps/daemon/src/reconnect.integration.test.ts apps/daemon/src/live-coordination.integration.test.ts",
    "test:watch": "vitest",
    "crosscode": "tsx apps/cli/src/index.ts",
    "daemon": "tsx apps/daemon/src/main.ts",
    "mcp": "tsx apps/mcp-server/src/main.ts",
    "service": "tsx apps/service/src/main.ts",
    "service:migrate": "tsx apps/service/src/migrate.ts",
    "service:provision": "tsx apps/service/src/provision-admin.ts",
    "docs:dev": "pnpm --filter @crosscode/docs-site dev",
    "docs:build": "pnpm --filter @crosscode/docs-site build",
    "docs:preview": "pnpm --filter @crosscode/docs-site preview"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@vitest/coverage-v8": "^3.2.4",
    "tsx": "^4.19.3",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
